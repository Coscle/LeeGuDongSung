spring:
  application:
    name: semiProject1

  datasource:
    url: jdbc:mysql://localhost:3306/mc
    username: mc
    password: mc
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
      
      
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.myweb.user